const express = require('express')
const app = express()
const port = 8080

app.post('/gen-proof', (req, res)=>{
    
})

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`)
})